# HTTP port for the console
PORT=3000

# Cron expression for periodic ingestion (default: every 30 minutes)
INGEST_CRON=*/30 * * * *

# Optional: NVD API key raises the request-rate limit for CVE ingestion.
# Free key: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=
